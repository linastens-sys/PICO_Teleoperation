# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Built by setup.sh against this machine's installed PC service -- don't
# vendor a prebuilt binary that might not match another machine's version.
/Pico-Pybind/lib/*.so
/Pico-Pybind/build/

# Downloaded installer, machine-specific.
*.deb
